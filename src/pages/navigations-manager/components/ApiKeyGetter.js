import PropTypes from 'prop-types';

const ApiKeyGetter = ({ setApiKey }) => {
    const getGoogleApiKey = () => {};

    return <></>;
};

ApiKeyGetter.propTypes = {
    children: PropTypes.node
};
export default ApiKeyGetter;
