import MapWithRoute from './MapPage';

const PetTracker = () => {
    return (
        <>
            <MapWithRoute real_time={true} title={'Real Time Tracker'} />
        </>
    );
};

export default PetTracker;
